apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-datasources-azuredisk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: azuredisk-sc
  resources:
    requests:
      storage: 100M